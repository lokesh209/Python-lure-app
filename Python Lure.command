#!/usr/bin/env bash
# Double-click this file in Finder to launch Python Lure (dev mode).
# (When you've built the .app via scripts/build_app.sh, use that instead.)
cd "$(dirname "$0")"
exec ./scripts/desktop.sh
